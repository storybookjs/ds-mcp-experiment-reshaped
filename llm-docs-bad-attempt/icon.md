# Icon Component

## Overview
The Icon component displays SVG icons with consistent sizing, coloring, and alignment throughout the design system, providing a unified approach to iconography with accessibility support.

## Key Features
- SVG icon rendering
- Consistent sizing and coloring
- Responsive icon dimensions
- Theme-aware colors
- Accessibility support
- Performance optimized

## Props Interface
```typescript
type IconProps = {
  svg: React.ReactElement | React.ComponentType;
  size?: Responsive<number | string>;
  color?: 'neutral' | 'neutral-faded' | 'positive' | 'critical' | 'warning' | 'primary' | 'disabled';
  autoWidth?: boolean;
  className?: string;
  attributes?: React.HTMLAttributes<HTMLSpanElement>;
};
```

## Usage Examples

### Basic Icon Usage
```typescript
import { Icon, View, Text } from 'reshaped';
import { HomeIcon, UserIcon, SettingsIcon } from './icons';\n\nfunction BasicIcons() {\n  return (\n    <View direction=\"row\" gap={4} align=\"center\">\n      <Icon svg={HomeIcon} />\n      <Icon svg={UserIcon} color=\"primary\" />\n      <Icon svg={SettingsIcon} size={6} />\n    </View>\n  );\n}\n```\n\n### Different Icon Sizes\n```typescript\nimport { Icon, View, Text } from 'reshaped';\nimport { StarIcon } from './icons';\n\nfunction IconSizes() {\n  const sizes = [3, 4, 5, 6, 8, 10];\n  \n  return (\n    <View gap={4}>\n      <Text variant=\"title-5\">Icon Sizes</Text>\n      <View direction=\"row\" gap={4} align=\"center\">\n        {sizes.map(size => (\n          <View key={size} gap={2} align=\"center\">\n            <Icon svg={StarIcon} size={size} />\n            <Text variant=\"body-3\">{size}px</Text>\n          </View>\n        ))}\n      </View>\n    </View>\n  );\n}\n```\n\n### Color Variants\n```typescript\nimport { Icon, View, Text } from 'reshaped';\nimport { AlertIcon } from './icons';\n\nfunction IconColors() {\n  const colors = [\n    { name: 'Neutral', value: 'neutral' },\n    { name: 'Primary', value: 'primary' },\n    { name: 'Positive', value: 'positive' },\n    { name: 'Warning', value: 'warning' },\n    { name: 'Critical', value: 'critical' },\n    { name: 'Disabled', value: 'disabled' }\n  ] as const;\n  \n  return (\n    <View gap={4}>\n      <Text variant=\"title-5\">Icon Colors</Text>\n      <View direction=\"row\" gap={6} align=\"center\">\n        {colors.map(({ name, value }) => (\n          <View key={value} gap={2} align=\"center\">\n            <Icon svg={AlertIcon} color={value} size={5} />\n            <Text variant=\"body-3\">{name}</Text>\n          </View>\n        ))}\n      </View>\n    </View>\n  );\n}\n```\n\n### Icons in Buttons\n```typescript\nimport { Icon, Button, View } from 'reshaped';\nimport { SaveIcon, DeleteIcon, EditIcon, ShareIcon } from './icons';\n\nfunction IconButtons() {\n  return (\n    <View gap={4}>\n      <View direction=\"row\" gap={3}>\n        <Button>\n          <Icon svg={SaveIcon} />\n          Save\n        </Button>\n        \n        <Button color=\"critical\">\n          <Icon svg={DeleteIcon} />\n          Delete\n        </Button>\n        \n        <Button variant=\"outline\">\n          <Icon svg={EditIcon} />\n          Edit\n        </Button>\n      </View>\n      \n      {/* Icon-only buttons */}\n      <View direction=\"row\" gap={2}>\n        <Button variant=\"ghost\" size=\"small\" aria-label=\"Share\">\n          <Icon svg={ShareIcon} />\n        </Button>\n        \n        <Button variant=\"ghost\" size=\"small\" aria-label=\"Edit\">\n          <Icon svg={EditIcon} />\n        </Button>\n        \n        <Button variant=\"ghost\" size=\"small\" aria-label=\"Delete\">\n          <Icon svg={DeleteIcon} />\n        </Button>\n      </View>\n    </View>\n  );\n}\n```\n\n### Responsive Icon Sizes\n```typescript\nimport { Icon, View, Text } from 'reshaped';\nimport { NotificationIcon } from './icons';\n\nfunction ResponsiveIcon() {\n  return (\n    <View gap={4}>\n      <Text variant=\"title-5\">Responsive Icon</Text>\n      <Icon \n        svg={NotificationIcon}\n        size={{ mobile: 4, tablet: 5, desktop: 6 }}\n        color=\"primary\"\n      />\n      <Text variant=\"body-3\">\n        This icon changes size based on screen size\n      </Text>\n    </View>\n  );\n}\n```\n\n### Icons with Text\n```typescript\nimport { Icon, Text, View } from 'reshaped';\nimport { CheckIcon, ErrorIcon, WarningIcon, InfoIcon } from './icons';\n\nfunction IconWithText() {\n  const statuses = [\n    { icon: CheckIcon, color: 'positive', text: 'Success message' },\n    { icon: ErrorIcon, color: 'critical', text: 'Error message' },\n    { icon: WarningIcon, color: 'warning', text: 'Warning message' },\n    { icon: InfoIcon, color: 'primary', text: 'Info message' }\n  ] as const;\n  \n  return (\n    <View gap={3}>\n      {statuses.map(({ icon, color, text }) => (\n        <View key={text} direction=\"row\" align=\"center\" gap={3}>\n          <Icon svg={icon} color={color} size={5} />\n          <Text>{text}</Text>\n        </View>\n      ))}\n    </View>\n  );\n}\n```\n\n### Custom Icon Component\n```typescript\nimport { Icon } from 'reshaped';\n\n// Custom SVG icon component\nfunction CustomIcon() {\n  const customSvg = (\n    <svg viewBox=\"0 0 24 24\" fill=\"currentColor\">\n      <path d=\"M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z\"/>\n    </svg>\n  );\n  \n  return (\n    <Icon \n      svg={customSvg}\n      size={6}\n      color=\"positive\"\n    />\n  );\n}\n```\n\n### Auto-width Icons\n```typescript\nimport { Icon, View, Text } from 'reshaped';\n\nfunction AutoWidthIcon() {\n  // Icon that maintains aspect ratio\n  const logoIcon = (\n    <svg viewBox=\"0 0 120 40\" fill=\"currentColor\">\n      <text x=\"10\" y=\"25\" fontSize=\"24\">LOGO</text>\n    </svg>\n  );\n  \n  return (\n    <View gap={3}>\n      <Text variant=\"title-6\">Auto-width Icon</Text>\n      <Icon \n        svg={logoIcon}\n        size={8}\n        autoWidth\n        color=\"primary\"\n      />\n    </View>\n  );\n}\n```\n\n## Icon Integration\n- Import SVG icons as React components\n- Use icon libraries (e.g., Heroicons, Lucide)\n- Custom SVG elements\n- Icon fonts (not recommended)\n\n## Sizing System\n- Size values in design system units\n- Responsive size configuration\n- Consistent scaling across components\n- Auto-width for non-square icons\n\n## Color System\n- Theme-aware color tokens\n- Semantic color meanings\n- Automatic contrast handling\n- Dark mode compatibility\n\n## Accessibility\n- Decorative icons (aria-hidden=\"true\")\n- Meaningful icons need aria-label\n- Screen reader compatibility\n- High contrast support\n\n## Performance\n- SVG optimization recommended\n- Icon sprite sheets for large sets\n- Tree-shaking friendly imports\n- Minimal DOM footprint\n\n## Related Components\n- **Button**: Icons within interactive elements\n- **Text**: Icons alongside text content"