# useToggle Hook

## Overview
The useToggle hook manages boolean toggle state with convenience methods for toggling, enabling, and disabling states, providing a clean API for binary state management.

## Key Features
- Boolean state management
- Convenient toggle methods
- Explicit enable/disable functions
- TypeScript support
- Performance optimized

## Hook Interface
```typescript
function useToggle(\n  initialValue: boolean = false\n): [\n  boolean,\n  {\n    toggle: () => void;\n    enable: () => void;\n    disable: () => void;\n    set: (value: boolean) => void;\n  }\n];\n```\n\n## Usage Examples\n\n### Basic Toggle Functionality\n```typescript\nimport { useToggle, Button, Text, View, Card } from 'reshaped';\n\nfunction BasicToggle() {\n  const [isEnabled, { toggle, enable, disable, set }] = useToggle(false);\n  \n  return (\n    <Card padding={6}>\n      <View gap={4}>\n        <Text variant=\"title-5\">Basic Toggle Demo</Text>\n        \n        <View \n          padding={4}\n          style={{\n            backgroundColor: isEnabled \n              ? 'var(--rs-color-background-positive-faded)' \n              : 'var(--rs-color-background-neutral-faded)',\n            borderRadius: '8px',\n            border: `2px solid ${isEnabled \n              ? 'var(--rs-color-border-positive)' \n              : 'var(--rs-color-border-neutral-faded)'\n            }`,\n            transition: 'all 0.3s ease'\n          }}\n        >\n          <Text variant=\"body-2\">\n            Status: <Text as=\"span\" weight=\"bold\">\n              {isEnabled ? 'Enabled' : 'Disabled'}\n            </Text>\n          </Text>\n        </View>\n        \n        <View direction=\"row\" gap={2}>\n          <Button onClick={toggle}>\n            Toggle ({isEnabled ? 'Disable' : 'Enable'})\n          </Button>\n          <Button \n            onClick={enable} \n            disabled={isEnabled}\n            variant=\"outline\"\n          >\n            Enable\n          </Button>\n          <Button \n            onClick={disable} \n            disabled={!isEnabled}\n            variant=\"outline\"\n          >\n            Disable\n          </Button>\n        </View>\n        \n        <View direction=\"row\" gap={2}>\n          <Button size=\"small\" onClick={() => set(true)}>\n            Set True\n          </Button>\n          <Button size=\"small\" onClick={() => set(false)}>\n            Set False\n          </Button>\n        </View>\n      </View>\n    </Card>\n  );\n}\n```\n\n### Theme Switcher\n```typescript\nimport { useToggle, Button, Text, View, Card } from 'reshaped';\nimport { useEffect } from 'react';\n\nfunction ThemeSwitcher() {\n  const [isDarkMode, { toggle, enable: enableDark, disable: enableLight }] = useToggle();\n  \n  // Apply theme to document\n  useEffect(() => {\n    document.documentElement.setAttribute(\n      'data-theme', \n      isDarkMode ? 'dark' : 'light'\n    );\n    \n    // Update CSS custom properties\n    document.documentElement.style.setProperty(\n      '--demo-bg',\n      isDarkMode ? '#1a1a1a' : '#ffffff'\n    );\n    document.documentElement.style.setProperty(\n      '--demo-text',\n      isDarkMode ? '#ffffff' : '#000000'\n    );\n  }, [isDarkMode]);\n  \n  return (\n    <Card padding={6}>\n      <View gap={4}>\n        <View direction=\"row\" justify=\"space-between\" align=\"center\">\n          <Text variant=\"title-5\">\n            Theme: {isDarkMode ? '🌙 Dark' : '☀️ Light'}\n          </Text>\n          <Button \n            onClick={toggle}\n            variant={isDarkMode ? 'solid' : 'outline'}\n          >\n            {isDarkMode ? '🌙' : '☀️'} Switch to {isDarkMode ? 'Light' : 'Dark'}\n          </Button>\n        </View>\n        \n        <div \n          style={{\n            padding: '16px',\n            backgroundColor: 'var(--demo-bg)',\n            color: 'var(--demo-text)',\n            borderRadius: '8px',\n            border: '1px solid var(--rs-color-border-neutral-faded)',\n            transition: 'all 0.3s ease'\n          }}\n        >\n          <Text variant=\"body-2\">\n            This area demonstrates the theme switching. The background and text \n            colors change based on the current theme selection.\n          </Text>\n        </div>\n        \n        <View direction=\"row\" gap={2} justify=\"center\">\n          <Button \n            size=\"small\" \n            onClick={enableLight}\n            variant={!isDarkMode ? 'solid' : 'ghost'}\n          >\n            ☀️ Light\n          </Button>\n          <Button \n            size=\"small\" \n            onClick={enableDark}\n            variant={isDarkMode ? 'solid' : 'ghost'}\n          >\n            🌙 Dark\n          </Button>\n        </View>\n      </View>\n    </Card>\n  );\n}\n```\n\n### Collapsible Content\n```typescript\nimport { useToggle, Button, Text, View, Card } from 'reshaped';\n\nfunction CollapsibleContent() {\n  const [isExpanded, { toggle, enable: expand, disable: collapse }] = useToggle(false);\n  \n  const content = [\n    \"This is the first paragraph of expandable content.\",\n    \"Here's some additional information that's hidden by default.\",\n    \"You can add as much content as needed here.\",\n    \"The content smoothly expands and collapses with animations.\",\n    \"This provides a better user experience with progressive disclosure.\"\n  ];\n  \n  return (\n    <Card padding={6}>\n      <View gap={4}>\n        <View direction=\"row\" justify=\"space-between\" align=\"center\">\n          <Text variant=\"title-5\">Article Title</Text>\n          <Button \n            size=\"small\" \n            variant=\"ghost\"\n            onClick={toggle}\n          >\n            {isExpanded ? '▼ Collapse' : '▶ Expand'}\n          </Button>\n        </View>\n        \n        <Text variant=\"body-2\">\n          This is the preview text that's always visible. Click expand to see more content.\n        </Text>\n        \n        <div\n          style={{\n            maxHeight: isExpanded ? '1000px' : '0',\n            overflow: 'hidden',\n            transition: 'max-height 0.3s ease, opacity 0.3s ease',\n            opacity: isExpanded ? 1 : 0\n          }}\n        >\n          <View gap={3}>\n            {content.map((paragraph, index) => (\n              <Text key={index} variant=\"body-3\">\n                {paragraph}\n              </Text>\n            ))}\n          </View>\n        </div>\n        \n        {isExpanded && (\n          <View direction=\"row\" gap={2}>\n            <Button size=\"small\" onClick={collapse}>\n              Collapse\n            </Button>\n            <Text variant=\"body-3\" color=\"neutral-faded\">\n              Expanded view active\n            </Text>\n          </View>\n        )}\n      </View>\n    </Card>\n  );\n}\n```\n\n### Feature Flags\n```typescript\nimport { useToggle, Button, Text, View, Card } from 'reshaped';\n\nfunction FeatureFlags() {\n  const [betaFeatures, betaControls] = useToggle(false);\n  const [notifications, notificationControls] = useToggle(true);\n  const [analytics, analyticsControls] = useToggle(false);\n  const [darkMode, darkModeControls] = useToggle(false);\n  \n  const features = [\n    {\n      name: 'Beta Features',\n      description: 'Enable experimental features and improvements',\n      enabled: betaFeatures,\n      controls: betaControls\n    },\n    {\n      name: 'Push Notifications',\n      description: 'Receive notifications about important updates',\n      enabled: notifications,\n      controls: notificationControls\n    },\n    {\n      name: 'Usage Analytics',\n      description: 'Help improve the app by sharing usage data',\n      enabled: analytics,\n      controls: analyticsControls\n    },\n    {\n      name: 'Dark Mode',\n      description: 'Use dark theme throughout the application',\n      enabled: darkMode,\n      controls: darkModeControls\n    }\n  ];\n  \n  const enabledCount = features.filter(f => f.enabled).length;\n  \n  return (\n    <Card padding={6}>\n      <View gap={6}>\n        <View gap={2}>\n          <Text variant=\"title-5\">Feature Settings</Text>\n          <Text variant=\"body-3\" color=\"neutral-faded\">\n            {enabledCount} of {features.length} features enabled\n          </Text>\n        </View>\n        \n        <View gap={4}>\n          {features.map((feature) => (\n            <View key={feature.name} direction=\"row\" justify=\"space-between\" align=\"center\">\n              <View gap={1} grow>\n                <Text variant=\"body-2\" weight=\"medium\">\n                  {feature.name}\n                </Text>\n                <Text variant=\"body-3\" color=\"neutral-faded\">\n                  {feature.description}\n                </Text>\n              </View>\n              \n              <View direction=\"row\" gap={2} align=\"center\">\n                <Text \n                  variant=\"body-3\" \n                  color={feature.enabled ? 'positive' : 'neutral-faded'}\n                >\n                  {feature.enabled ? 'On' : 'Off'}\n                </Text>\n                \n                <Button \n                  size=\"small\"\n                  variant={feature.enabled ? 'solid' : 'outline'}\n                  onClick={feature.controls.toggle}\n                >\n                  {feature.enabled ? 'Disable' : 'Enable'}\n                </Button>\n              </View>\n            </View>\n          ))}\n        </View>\n        \n        <View direction=\"row\" gap={2}>\n          <Button \n            size=\"small\" \n            onClick={() => {\n              features.forEach(f => f.controls.enable());\n            }}\n            disabled={enabledCount === features.length}\n          >\n            Enable All\n          </Button>\n          \n          <Button \n            size=\"small\" \n            variant=\"outline\"\n            onClick={() => {\n              features.forEach(f => f.controls.disable());\n            }}\n            disabled={enabledCount === 0}\n          >\n            Disable All\n          </Button>\n          \n          <Button \n            size=\"small\" \n            variant=\"ghost\"\n            onClick={() => {\n              // Reset to defaults\n              betaControls.disable();\n              notificationControls.enable();\n              analyticsControls.disable();\n              darkModeControls.disable();\n            }}\n          >\n            Reset to Defaults\n          </Button>\n        </View>\n      </View>\n    </Card>\n  );\n}\n```\n\n### Sidebar Toggle\n```typescript\nimport { useToggle, Button, Text, View, Card } from 'reshaped';\nimport { useEffect } from 'react';\n\nfunction SidebarToggle() {\n  const [sidebarOpen, { toggle, disable: closeSidebar }] = useToggle(false);\n  \n  // Close sidebar on escape key\n  useEffect(() => {\n    const handleEscape = (event: KeyboardEvent) => {\n      if (event.key === 'Escape' && sidebarOpen) {\n        closeSidebar();\n      }\n    };\n    \n    document.addEventListener('keydown', handleEscape);\n    return () => document.removeEventListener('keydown', handleEscape);\n  }, [sidebarOpen, closeSidebar]);\n  \n  const sidebarContent = [\n    'Navigation Item 1',\n    'Navigation Item 2', \n    'Navigation Item 3',\n    'Settings',\n    'Help & Support',\n    'Sign Out'\n  ];\n  \n  return (\n    <View>\n      {/* Header */}\n      <Card \n        padding={4}\n        style={{ \n          borderBottom: '1px solid var(--rs-color-border-neutral-faded)'\n        }}\n      >\n        <View direction=\"row\" justify=\"space-between\" align=\"center\">\n          <Button \n            variant=\"ghost\" \n            size=\"small\"\n            onClick={toggle}\n          >\n            {sidebarOpen ? '✕' : '☰'} Menu\n          </Button>\n          <Text variant=\"title-5\">App Header</Text>\n          <div style={{ width: '60px' }} /> {/* Spacer */}\n        </View>\n      </Card>\n      \n      <View direction=\"row\" style={{ position: 'relative' }}>\n        {/* Sidebar Overlay */}\n        {sidebarOpen && (\n          <div\n            style={{\n              position: 'fixed',\n              top: 0,\n              left: 0,\n              right: 0,\n              bottom: 0,\n              backgroundColor: 'rgba(0, 0, 0, 0.5)',\n              zIndex: 999\n            }}\n            onClick={closeSidebar}\n          />\n        )}\n        \n        {/* Sidebar */}\n        <div\n          style={{\n            position: 'fixed',\n            top: 0,\n            left: sidebarOpen ? 0 : '-280px',\n            width: '280px',\n            height: '100vh',\n            backgroundColor: 'var(--rs-color-background-elevation-base)',\n            borderRight: '1px solid var(--rs-color-border-neutral-faded)',\n            transition: 'left 0.3s ease',\n            zIndex: 1000,\n            paddingTop: '80px' // Account for header height\n          }}\n        >\n          <Card padding={4}>\n            <View gap={3}>\n              <Text variant=\"title-6\">Navigation</Text>\n              \n              <View gap={1}>\n                {sidebarContent.map((item, index) => (\n                  <Button\n                    key={index}\n                    variant=\"ghost\"\n                    onClick={() => {\n                      console.log('Navigate to:', item);\n                      closeSidebar();\n                    }}\n                    style={{\n                      width: '100%',\n                      justifyContent: 'flex-start'\n                    }}\n                  >\n                    {item}\n                  </Button>\n                ))}\n              </View>\n              \n              <Text variant=\"body-3\" color=\"neutral-faded\">\n                Press Escape or click outside to close\n              </Text>\n            </View>\n          </Card>\n        </div>\n        \n        {/* Main Content */}\n        <View grow padding={6}>\n          <Card padding={6}>\n            <View gap={4}>\n              <Text variant=\"title-4\">Main Content Area</Text>\n              \n              <Text variant=\"body-2\">\n                Sidebar is currently {sidebarOpen ? 'open' : 'closed'}. \n                Use the menu button to toggle the sidebar.\n              </Text>\n              \n              <View direction=\"row\" gap={2}>\n                <Button onClick={toggle}>\n                  {sidebarOpen ? 'Close' : 'Open'} Sidebar\n                </Button>\n                \n                {sidebarOpen && (\n                  <Text variant=\"body-3\" color=\"positive\">\n                    ✓ Sidebar is open\n                  </Text>\n                )}\n              </View>\n            </View>\n          </Card>\n        </View>\n      </View>\n    </View>\n  );\n}\n```\n\n## API Methods\n\n### toggle()\nToggles between true and false states\n```typescript\nconst [state, { toggle }] = useToggle();\n// state: false → true → false\n```\n\n### enable()\nSets state to true\n```typescript\nconst [state, { enable }] = useToggle();\n// state: false → true\n```\n\n### disable()\nSets state to false\n```typescript\nconst [state, { disable }] = useToggle();\n// state: true → false\n```\n\n### set(value)\nSets state to specific boolean value\n```typescript\nconst [state, { set }] = useToggle();\nset(true);  // state: true\nset(false); // state: false\n```\n\n## Use Cases\n\n### UI State Management\n- Modal open/close states\n- Sidebar visibility\n- Accordion expand/collapse\n- Theme switching\n- Feature toggles\n\n### Form Controls\n- Checkbox state management\n- Toggle switches\n- Show/hide password fields\n- Terms and conditions acceptance\n\n## Performance\n- Lightweight implementation\n- Stable function references\n- No unnecessary re-renders\n- Memory efficient\n\n## Best Practices\n- Use descriptive variable names\n- Prefer explicit methods (enable/disable) over toggle for clarity\n- Combine with useEffect for side effects\n- Consider initial state based on user preferences\n- Test toggle interactions thoroughly\n\n## Related Hooks\n- **useState**: For more complex state management\n- **useReducer**: For complex state transitions\n- **useCallback**: For memoizing toggle functions"